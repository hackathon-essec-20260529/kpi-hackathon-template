# Tournament: route LLM calls through the metering proxy with your team's
# virtual key. Set both — they take precedence over GROQ_API_KEY below.
LLM_BASE_URL=https://llm-proxy-71nz.onrender.com/v1
LLM_API_KEY=

# Local dev only: call Groq directly (used when LLM_API_KEY is unset).
GROQ_API_KEY=
